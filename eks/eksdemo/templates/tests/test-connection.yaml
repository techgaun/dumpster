apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "eksdemo.fullname" . }}-test-connection"
  labels:
{{ include "eksdemo.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "eksdemo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
